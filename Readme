1. NAME
       youtube_cache - Squid redirector plugin for caching Youtube Videos

2. SYNOPSIS
       To be used to with squid as a redirector plugin.

3. DESCRIPTION
       youtube_cache is a squid redirecto plugin written in Python to facilitate youtube video caching. It can cache youtube videos in a sep-
       arate directory (other than squid cache) in a browsable fashion and can serve the subsequent requests from the cache. It helps in sav-
       ing bandwidth and loading time.

4. CONFIGURATION
       To configure youtube_cache, you need to configure Apache Webserver (httpd), Proxy Server (squid), and the global youtube_cache config-
       uration file. Below are the steps to configure youtube_cache.

4.1 Squid Configuration
       To configure squid to use, youtube_cache as a redirector, you need to add the following lines to your sqiud configuration file (gener-
       ally found in /etc/squid/squid.conf )

       # --BEGIN-- youtube_cache config #
       redirect_program /usr/bin/python /etc/squid/youtube_cache/youtube_cache.py
       redirect_children 20
       # --END-- youtube_cache config #

4.2 Apache Webserver (httpd) Configuration
       Config file : /etc/httpd/conf.d/youtube_cache.conf
       Modify the configuration file the way you want and reload the httpd service using the following command.

       [root@localhost root]# service httpd reload

4.3 youtube_cache Global Configuration
       Config file : /etc/sysconfig/youtube_cache.conf

       cache_dir
              Directory to cache youtube videos.
              Default : /var/spool/squid/youtube/

       temp_dir
              Directory to store partitially cached youtube videos.
              Default : /var/spool/sqiud/youtube/temp

       http_proxy
              Proxy server to use for caching youtube videos.
              Default : http://localhost.localdomain:3128/

       cache_url
              URL to serve the cached youtube videos. If you change this, don’t forget to modify /etc/httpd/conf.d/youtube_cache.conf accord-
              ingly and reload the httpd service.
              Default : http://localhost.localdomain/youtube/

       logfile
              File to log all the youtube_cache activities.
              Default : /var/log/squid/youtube_cache.log

5. FILES
       /etc/sysconfig/youtube_cache.conf
       /etc/httpd/conf.d/youtube_cache.conf
       /etc/squid/youtube_cache/
       /var/spool/squid/youtube/
       /var/log/squid/youtube_cache.log

6. SEE ALSO
       squid (8)
       http://fedora.co.in/
       How to configure squid : http://fedora.co.in/content/how-configure-squid-proxy-server
       How to write custorm squid redirector in python : http://fedora.co.in/content/writing-custom-redirector-plugin-squid-python

7. AUTHORS
       Kulbir Saini <kulbirsaini@students.iiit.ac.in>

8. BUGS
       Mail bugs/suggestions to Kulbir Saini <kulbirsaini@students.iiit.ac.in>

